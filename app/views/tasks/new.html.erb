<h1>新規タスクの作成</h1>

<%= form_with(model: @task, local: true) do |f| %>
    <%= f.label :content, "タスク" %>
    <%= f.text_field :content %>
    
    <%= f.submit "作成する" %>
<% end %>

<%= link_to "タスク一覧に戻る", tasks_path %>