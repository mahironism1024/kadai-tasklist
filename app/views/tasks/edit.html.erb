<h1>id: <%= @task.id %> のタスクを編集します</h1>

<%= form_with(model: @task, local: true) do |f| %>
    <%= f.label :content, "タスク" %>
    <%= f.text_field :content %>
    
    <%= f.submit "登録", data: {confirm: "編集内容を登録してよろしいですか？"} %>
<% end %>

<%= link_to "タスク一覧に戻る", tasks_path %>